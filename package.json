{
  "name": "prospector-ai-root",
  "version": "1.0.0",
  "description": "Redirige el despliegue de Railway a la carpeta v2/backend",
  "scripts": {
    "build": "cd v2/backend && npm install",
    "start": "cd v2/backend && npm start"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
